# Copy this file to .kamal/secrets and fill in the values.
# .kamal/secrets is gitignored and must never be committed.
#
# Generate a secret key: bin/rails secret

SECRET_KEY_BASE=changeme_run_bin_rails_secret
SMTP_USERNAME=your_smtp_username
SMTP_PASSWORD=your_smtp_password
